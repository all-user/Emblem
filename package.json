{
  "name": "@all-user/ok-blocks",
  "version": "0.1.3",
  "description": "Generate blocks consists of 3x3 elements.",
  "main": "lib/index.js",
  "scripts": {
    "help": "npm-run-help",
    "test": "testem",
    "flow": "flow; test $? -eq 0 -o $? -eq 2",
    "lint": "run-p lint:*",
    "lint:src": "eslint src/**/*.js",
    "lint:test": "eslint test/**/*.test.js",
    "build:js": "babel src -x .js -d lib",
    "build:browser": "browserify -t babelify -r ./src/index.js:@all-user/ok-blocks | uglifyjs > ./dist/bundle.js",
    "build:copy": "run-p build:copy:*",
    "build:copy:flow": "cpx \"src/*.js.flow\" lib",
    "watch:js": "babel -w src -x .js -d lib",
    "watch:copy": "run-p watch:copy:*",
    "watch:copy:flow": "cpx -w \"src/*.js.flow\" lib",
    "clean": "run-s clean:*",
    "clean:lib": "rimraf lib && mkdirp lib",
    "clean:browser": "rimraf dist && mkdirp dist",
    "release": "run-s clean build",
    "build": "run-s build:*",
    "watch": "run-p watch:*",
    "start": "npm run watch"
  },
  "author": "Keita Okamoto <ok@aaa.email.ne.jp>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-eslint": "^7.2.3",
    "babel-preset-env": "^1.6.0",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-power-assert": "^1.0.0",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "cpx": "^1.5.0",
    "eslint": "^4.4.1",
    "eslint-plugin-flowtype": "^2.35.0",
    "eslint-plugin-import": "^2.7.0",
    "exorcist": "^0.4.0",
    "flow-bin": "^0.52.0",
    "flow-typed": "^2.1.5",
    "mkdirp": "^0.5.1",
    "mocha": "^3.5.0",
    "nib": "^1.1.2",
    "npm-run-all": "^4.0.2",
    "npm-run-help": "^1.1.0",
    "power-assert": "^1.4.4",
    "rimraf": "^2.6.1",
    "stylus": "^0.54.5",
    "testem": "^1.18.3",
    "uglify-js": "^3.0.27",
    "watch": "^1.0.2",
    "watchify": "^3.9.0"
  },
  "dependencies": {}
}
